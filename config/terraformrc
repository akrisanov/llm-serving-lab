# Terraform configuration for Russia-based users
# This configuration enables local filesystem mirror for Yandex Cloud provider
# to improve connectivity and avoid potential DNS/network issues.
#
# Usage:
#   cp config/terraformrc ~/.terraformrc
#   mkdir -p ~/.local/terraform-providers
#
# The provider will be automatically downloaded to the local mirror
# on first terraform init and reused in subsequent runs.

provider_installation {
  filesystem_mirror {
    path    = "/Users/akrisanov/.local/terraform-providers"
    include = ["registry.terraform.io/yandex-cloud/yandex"]
  }
  direct {
    exclude = ["registry.terraform.io/yandex-cloud/yandex"]
  }
}
