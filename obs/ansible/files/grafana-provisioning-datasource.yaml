apiVersion: 1
datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    access: proxy
    isDefault: true
    editable: true
    jsonData:
      server: "http://clickhouse:8123"
      defaultDatabase: "${CH_DB}"
